# AGENTS.md — bai-shared 저장소 안내 (AI 에이전트용)

이 파일은 사람이 아니라 **AI 코딩 에이전트**(Claude Code, Codex, Cursor 등)가 읽는 안내입니다. 팀원이 이 저장소를 열어두고 아래 같은 요청을 하면, 그때마다 이 절차를 따르세요.

## 저장 위치 원칙 (중요 — clone/sparse-checkout 전에 항상 확인)

clone이나 sparse-checkout을 실행하기 전에, 먼저 목적이 "확인용"인지 "계속 쓸 용도"인지 구분합니다.

- **기본값(확인·조회 목적)**: "받아와"·"동기화해줘"·"OO 작업만 받고 싶어"처럼 지금 상황을 확인만 하려는 요청이면, 이 AI 도구가 제공하는 **세션 임시 폴더(스크래치패드 등)**에 clone합니다. 그런 개념이 없는 도구라면 시스템 임시 폴더(`%TEMP%`, `/tmp` 등)를 씁니다. 사용자 컴퓨터의 일반 작업 폴더에 바로 만들지 않습니다.
- **영구 저장은 사용자가 명시적으로 요청할 때만**: "내 컴퓨터에 저장해줘", "이 폴더 계속 쓸 거야" 같은 요청이 있을 때, 그제서야 사용자가 원하는 위치(정해주지 않으면 먼저 물어봄)로 폴더를 복사·이동합니다.
- **예외 — "내 프로젝트 공유해줘"**: 이건 확인용이 아니라 사용자가 앞으로 계속 작업할 저장소이므로, 처음부터 사용자 컴퓨터의 실제 위치에 clone하는 게 자연스럽습니다. 어디에 둘지 확인 후 진행합니다.
- 애매하면 추측하지 말고 "이번엔 확인만 하실 건가요, 컴퓨터에 계속 두고 쓰실 건가요?"라고 물어봅니다.

## "받아와" / "동기화해줘" / "팀원들 작업 보여줘"라고 하면

새로 clone하는 경우와, 이미 받아본 적 있는 저장소를 다시 동기화하는 경우를 구분합니다. **핵심 원칙: 지난번에 이미 보여준 내용(핸드오프 문서든 프로젝트 파일이든)을 매번 다시 읽거나 다시 설명하지 않습니다.**

1. **현재 폴더가 이 저장소의 clone이 아니면** (`.git` 없음): 위 "저장 위치 원칙"에 따라 (기본은 세션 임시 폴더에서) `git clone https://github.com/trbb82349/bai-shared.git`을 실행합니다. 처음이라 전부 새 내용이므로, `handoffs/` 안의 문서를 전부 읽고 요약하고, `projects/` 아래 폴더 목록도 한 번 훑어서 보여줍니다.
2. **이미 clone되어 있으면**, 새로 추가되거나 바뀐 것만 골라냅니다.
   a. `git pull` 실행 — pull(merge)을 하면 git이 자동으로 직전 커밋을 `ORIG_HEAD`에 기록해둡니다.
   b. **핸드오프 문서**: `git diff --name-status ORIG_HEAD HEAD -- handoffs/` 실행. 새로 추가(`A`)되거나 바뀐(`M`) 문서 목록만 나옵니다. 목록에 있는 파일만 열어서 요약합니다.
   c. **프로젝트 파일**: `git diff --name-status ORIG_HEAD HEAD -- projects/` 실행. 나온 경로들의 앞 두 단계(`projects/<사람이름>/<프로젝트이름>/...`의 `<사람이름>/<프로젝트이름>`)만 뽑아서 중복 없이 "이번에 업데이트된 프로젝트: trbb82349/careerlens-mvp (파일 3개 변경)"처럼 알려줍니다. 파일 하나하나를 다시 설명하지 않고, 어떤 사람의 어떤 프로젝트가 바뀌었는지만 짚어줍니다.
   d. 두 결과가 모두 비어 있으면 "새로 올라온 핸드오프 문서·프로젝트 업데이트가 없어요"라고 그대로 알려주고 끝냅니다.
3. 새로 나온 핸드오프 문서는 한국어로 짧게 요약해서 보여줍니다 — 7개 항목(기본 정보·목표·한 일/결정한 것·결과물 위치·막힌 것·다음 할 일·다른 파트와의 연결점) 중 **"막힌 것"과 "다음 할 일"을 가장 눈에 띄게** 보여주세요.
4. 사용자가 업데이트된 프로젝트나, 요약에 없던 오래된 프로젝트를 더 보고 싶다고 하면 그때 `projects/<사람이름>/<프로젝트이름>/README.md`와 관련 파일을 열어서 설명하고, 실행 방법(로컬 서버 필요 여부 등)을 그 프로젝트의 README에서 확인해 안내합니다.

## "OO(팀원 이름)의 작업만 받고 싶어" / "특정 팀원 프로젝트만 보고 싶어"라고 하면

전체를 다 받지 않고, git의 **sparse-checkout**(부분 체크아웃)으로 원하는 사람 폴더만 실제 디스크에 받습니다. `handoffs/`는 누가 뭘 하는지 파악하는 데 필요하니 항상 포함합니다.

0. 사용자가 말한 **이름**을 [MEMBERS.md](MEMBERS.md)에서 찾아 GitHub 아이디로 바꿉니다 (`projects/` 아래 폴더명은 이 GitHub 아이디 기준입니다). 표에 없는 이름이면 추측하지 말고 GitHub 아이디를 직접 물어봅니다.
1. **아직 clone한 적 없으면** (위 "저장 위치 원칙"에 따라 기본은 세션 임시 폴더):
   ```
   git clone --filter=blob:none --sparse https://github.com/trbb82349/bai-shared.git
   cd bai-shared
   git sparse-checkout set handoffs projects/<팀원GitHub아이디>
   ```
2. **이미 전체를 clone해뒀는데 범위만 좁히고 싶으면** (저장소 폴더 안에서):
   ```
   git sparse-checkout init --cone
   git sparse-checkout set handoffs projects/<팀원GitHub아이디>
   ```
3. 나중에 다른 사람 것도 보고 싶어지면 지우지 않고 추가만 하면 됩니다:
   ```
   git sparse-checkout add projects/<다른팀원GitHub아이디>
   ```
4. 다시 전체를 보고 싶으면:
   ```
   git sparse-checkout disable
   ```
5. **이후 다시 "받아와"라고 하면, "받아와" 절차와 똑같은 원칙(지난번 이후 새로운 것만)이 그대로 적용되되 확인 범위만 좁혀집니다**:
   a. `git pull` 실행 (ORIG_HEAD에 직전 커밋 기록됨)
   b. `git diff --name-status ORIG_HEAD HEAD -- handoffs/` — 새 핸드오프 문서 확인 (전체 공통)
   c. `git diff --name-status ORIG_HEAD HEAD -- projects/<선택한팀원GitHub아이디>/` — **`projects/` 전체가 아니라 지정한 그 사람 폴더로만 좁혀서** 확인합니다. 이렇게 해야 관심 범위 밖인 다른 사람의 변경사항을 불필요하게 알리지 않습니다.
   d. 둘 다 비어 있으면 "이 사람은 지난번 이후 새로 올라온 게 없어요"라고 알리고 끝냅니다.
6. 본인 프로젝트를 공유(push)할 때는 이 기능과 무관하게 항상 자기 폴더에만 올립니다 — sparse-checkout은 "받는 범위"만 좁히는 것이고 쓰기 권한과는 별개입니다.

## "내 브랜치로 연결해줘" / "내 작업 브랜치 시작해줘"라고 하면

프로젝트 파일을 옮기기 전에, 이번 작업용 브랜치로 전환만 먼저 해두고 싶을 때 씁니다. ("내 프로젝트 공유해줘" 절차의 1a와 같은 내용이라 공유할 때 자동으로도 실행되지만, 작업을 시작하는 시점에 미리 연결해두고 싶으면 이 절차만 따로 실행해도 됩니다.)

**브랜치는 사람 단위가 아니라 작업 단위입니다.** 이름 형식은 `<GitHub아이디>/<작업이름>` (예: `trbb82349/careerlens-update`, `chldbfk/project-a`). 같은 사람이 여러 작업을 동시에 진행해도 브랜치가 겹치지 않고, 작업이 끝나면(PR이 main에 merge되면) 그 브랜치는 자동으로 삭제됩니다.

0. 본인 GitHub 아이디를 정합니다. 이름을 말했으면 [MEMBERS.md](MEMBERS.md)에서 찾고, 표에 없으면(처음 연결하는 사람) GitHub 아이디를 물어보고 — `git config user.name`이나 `gh api user`로 추측되면 맞는지 확인만 받습니다 — 확인되면 [MEMBERS.md](MEMBERS.md)에 "이름 | GitHub 아이디" 줄을 새로 추가합니다.
0-1. 이번에 할 작업을 짧은 영어 kebab-case 이름으로 정합니다 (예: "careerlens 업데이트" → `careerlens-update`). 애매하면 사용자에게 한 줄로 물어봅니다. 브랜치 이름은 `<GitHub아이디>/<작업이름>`이 됩니다.
1. 아직 clone하지 않았으면 "저장 위치 원칙"에 따라 clone합니다. 이 절차는 대부분 "이어서 계속 작업할 용도"이므로, 애매하면 확인용인지 계속 쓸 폴더인지 먼저 물어봅니다.
2. `git fetch origin`
3. 원격에 같은 `<GitHub아이디>/<작업이름>` 브랜치가 이미 있으면(이어서 하는 작업): `git switch <GitHub아이디>/<작업이름>` (로컬에 없으면 `git switch -c <GitHub아이디>/<작업이름> origin/<GitHub아이디>/<작업이름>`), 이어서 `git pull`로 최신화합니다.
4. 없으면(새 작업): `git switch -c <GitHub아이디>/<작업이름> origin/main`으로 새로 만들고, `git push -u origin <GitHub아이디>/<작업이름>`로 원격에도 만들어둡니다.
5. `main`이 이 브랜치보다 앞서 있으면(다른 사람 PR이 먼저 merge된 경우) `git merge origin/main`으로 최신 내용을 받습니다. 충돌이 나면 임의로 풀지 말고 사용자에게 보여주고 같이 해결합니다.
6. 완료되면 "이제 `<GitHub아이디>/<작업이름>` 브랜치에 연결됐습니다"라고 짧게 알려줍니다. 이 단계에서는 프로젝트 파일을 옮기거나 PR을 열지 않습니다 — 그건 아래 "내 프로젝트 공유해줘"에서 진행합니다.

## "내 프로젝트 공유해줘" / "핸드오프 문서 써줘"라고 하면

**핵심 원칙: 바뀐 게 없으면 아무것도 새로 만들지 않습니다.** (빈 커밋, 의미 없는 핸드오프 문서 금지)

**공유는 `main`에 직접 push하지 않고, 본인 GitHub 아이디 브랜치 → `main`으로의 PR로 합니다.** (아래 1a 단계)

1. 사용자에게 어떤 프로젝트 폴더를 올릴지 확인합니다. 본인 GitHub 아이디는 다음 순서로 정합니다.
   - 사용자가 이름을 말했으면 [MEMBERS.md](MEMBERS.md)에서 그 이름을 찾아 GitHub 아이디로 바꿉니다.
   - 표에 이름이 없으면(처음 공유하는 사람), GitHub 아이디를 물어보고 — `git config user.name`이나 `gh api user`로 추측되면 맞는지 확인만 받습니다 — 확인되면 [MEMBERS.md](MEMBERS.md)에 "이름 | GitHub 아이디" 줄을 새로 추가합니다.
   1a. **본인 브랜치로 전환합니다** (파일을 옮기기 전에 먼저) — 위 "내 브랜치로 연결해줘" 절차와 동일합니다. 이미 그 브랜치에 있으면 이 단계는 건너뜁니다.
2. `projects/<사용자GitHub아이디>/<프로젝트이름>/`이 이 저장소에 이미 있는지 확인합니다.
   - **처음 올리는 프로젝트**면 폴더째 복사합니다. `projects/<사용자GitHub아이디>/` 폴더가 아직 없으면 새로 만듭니다.
   - **처음 올리는 프로젝트면 README.md가 있는지도 확인합니다.** 없으면 CI(`repo-rules-check`)가 PR을 막으므로, 무엇을 하는 프로젝트인지와 실행 방법을 짧게라도 적어서 `projects/<사용자GitHub아이디>/<프로젝트이름>/README.md`를 만듭니다.
   - **이미 있는 프로젝트(업데이트)**면, 로컬 원본 폴더 내용으로 덮어써서 다시 복사합니다. 이때 로컬에서는 지웠는데 저장소 쪽엔 남아있는 파일이 있으면, 같이 지울지 사용자에게 확인합니다 (안 그러면 안 쓰는 옛날 파일이 계속 쌓임).
   - 원본 로컬 폴더는 건드리지 않고 그대로 둡니다.
   - 다른 사람의 폴더(`projects/다른사람아이디/`)에는 올리거나 수정하지 않습니다.
3. 복사 직후 `git status`와 `git diff --stat -- projects/<사용자GitHub아이디>/<프로젝트이름>/`로 실제 추가·수정·삭제된 파일이 있는지 확인합니다.
   - **아무 변화가 없으면** 핸드오프 문서도 새로 만들지 않고, "이 프로젝트는 지난번 공유 이후 바뀐 게 없어요"라고 사용자에게 알리고 끝냅니다.
   - 변화가 있으면 다음 단계로 진행합니다.
4. `handoffs/template.md`를 복사해서 `handoffs/YYYY-MM-DD-이름-핸드오프.md`로 만들고, 사용자와 대화하며 7개 항목을 채웁니다.
   - 원본 대화 전체를 붙여넣지 않습니다. 반드시 항목별로 걸러서 요약합니다.
   - "결과물 위치"에는 로컬 경로가 아니라 이 저장소 안의 경로(`projects/사용자GitHub아이디/프로젝트이름/...`)를 적습니다.
   - `handoffs/`에 같은 프로젝트의 이전 문서가 있으면 찾아서, 그 문서의 "다음 할 일"이 이번에 얼마나 처리됐는지 짚어줍니다.
4-1. **로컬에서 직접 실행/테스트해봤는지 한 줄로 물어봅니다.** 해봤으면 어떻게 확인했는지(예: "`python -m http.server`로 띄워서 화면 확인"), 안 해봤으면 그 사실과 이유를 받아둡니다 — 이 답변을 7단계 PR 본문의 "실행 방법 / 테스트" 섹션에 그대로 씁니다.
5. `git add`, `git commit`으로 작업 브랜치(`<GitHub아이디>/<작업이름>`)에 커밋합니다. 1단계에서 [MEMBERS.md](MEMBERS.md)에 새 줄을 추가했다면 그 파일도 같이 커밋에 포함합니다. 올리기 전에 어떤 파일이 커밋되는지 사용자에게 보여주고 확인받습니다 (특히 `.env`, API 키, 큰 바이너리 파일이 섞여 있지 않은지 — 이 저장소는 public입니다).
6. `git push -u origin <GitHub아이디>/<작업이름>`로 작업 브랜치를 원격에 올립니다 (이미 추적 중이면 `git push`).
7. `main`으로의 PR을 엽니다. PR 본문은 `.github/pull_request_template.md`의 섹션 구조에 맞춰 직접 채웁니다 (`--body`를 넘기면 템플릿이 자동으로 채워지지 않으므로, 아래 내용을 사람이 채운 것처럼 직접 구성해서 넣어야 합니다).
   - **프로젝트**: `projects/<사용자GitHub아이디>/<프로젝트이름>`
   - **변경 내용**: 이번에 뭘 왜 바꿨는지 2~3줄
   - **실행 방법 / 테스트**: 4-1에서 받아둔 답변 그대로
   - **핸드오프 요약**: 4단계 핸드오프 문서의 핵심 2~3줄
   - `gh` CLI가 설치되어 있고 로그인되어 있으면(`gh auth status`): 위 내용을 채운 본문으로 `gh pr create --base main --head <GitHub아이디>/<작업이름> --title "<이름>: <프로젝트/변경 요약>" --body "<위 4개 섹션>"`을 실행하고, 만들어진 PR 링크를 사용자에게 알려줍니다. 이미 이 브랜치로 연 PR이 있으면(같은 head/base) 새로 만들지 않고 기존 PR에 커밋이 추가됐다고만 알려줍니다.
   - `gh`가 없거나 로그인 안 되어 있으면, 직접 열 수 있는 링크를 알려줍니다: `https://github.com/trbb82349/bai-shared/compare/main...<GitHub아이디>/<작업이름>?expand=1` (이 경우 웹 화면에서 템플릿이 자동으로 채워지므로, 위 내용을 그대로 빈칸에 채우라고 안내합니다)
8. **CI 검사(`folder-ownership-check`, `secret-scan`, `repo-rules-check`)가 실패하면**, PR 화면의 빨간 X를 보고 사용자에게 원인과 해결 방법을 바로 설명합니다.
   - `folder-ownership-check` 실패: 이 브랜치(`<GitHub아이디>/...`) 소유가 아닌 다른 사람의 `projects/` 폴더를 건드렸다는 뜻입니다. 정말 필요한 변경인지 확인하고, 아니면 그 파일을 커밋에서 빼야 합니다.
   - `secret-scan` 실패: 커밋 내용에 API 키·비밀번호처럼 보이는 문자열이 있다는 뜻입니다. 실제 키라면 해당 서비스에서 즉시 키를 폐기하고, git 히스토리에서도 제거해야 합니다.
   - `repo-rules-check` 실패: 로그에 표시된 파일을 보고 원인을 확인합니다 — (1) `.env`/키 파일 이름이 그대로 커밋됨, (2) `projects/<아이디>/<프로젝트명>/` 구조보다 얕은 위치에 파일을 둠, (3) 새 프로젝트인데 README.md가 없음, 중 하나입니다.
   - 원인을 고친 뒤 같은 브랜치에 다시 push하면 검사가 자동으로 재실행됩니다.
9. PR을 언제 merge할지(본인이 직접 merge할지, 다른 사람 리뷰를 기다릴지)는 사용자 판단이므로 임의로 merge하지 않습니다. PR이 merge되면 저장소 설정(Automatically delete head branches)에 따라 그 작업 브랜치는 자동으로 삭제됩니다. 로컬 브랜치는 `git switch main && git branch -d <GitHub아이디>/<작업이름>`로 정리해주면 됩니다.

## 실행 관련 주의사항

- 프로젝트마다 실행 방법이 다를 수 있습니다 (예: `careerlens-mvp`의 v2~v4 HTML은 `python -m http.server`로 열어야 하고 `file://`로 직접 열면 안 됨). 안내 전에 항상 그 프로젝트의 `README.md`를 먼저 확인하세요.
- 이 저장소는 **public**입니다. 비밀번호, API 키, 개인정보는 절대 올리지 않습니다.
- 잘 모르겠거나 애매한 요청은 추측해서 진행하지 말고 사용자에게 짧게 확인합니다.
